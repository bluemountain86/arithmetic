package com.wangb.arith.binarysearch.search;

/**
 * @Author wangbin
 * @Date 2021/1/26
 */
public class Search33_5 {
    public int search(int[] nums, int target) {
        int targetIndex = 0;

        int left = 0;
        int right = nums.length - 1;
//        [4,5,6,7,0,1,2]
        while (left <= right) {
            int mid = left + (right - left) / 2;
        }

        return targetIndex;
    }
}
